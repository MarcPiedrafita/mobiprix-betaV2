---
/* ==========================================================================
   Forat per a l'analítica
   --------------------------------------------------------------------------
   Buit a propòsit. El brief demana deixar-ho preparat però NO connectar cap
   servei real, i una demo que truqui a un tercer ja no és una demo tancada.

   Per endollar-ho més endavant (Plausible, Fathom, GA4… el que es decideixi):
   posar l'script aquí i llegir l'identificador d'una variable d'entorn, no
   escrit al codi:

     const id = import.meta.env.PUBLIC_ANALITICA_ID;
     {id && <script defer data-website-id={id} src="..."></script>}

   Mentre no hi hagi decisió de proveïdor ni de base legal per a les galetes,
   aquest component no pinta res.
   ========================================================================== */
---
