---
import { t, type Idioma } from '../i18n/utils';

interface Props {
  idioma: Idioma;
  estat: 'disponible' | 'reservat' | 'exhaurit';
}

const { idioma, estat } = Astro.props;
---

<span class={`estado estado--${estat}`}>{t(idioma, `estats.${estat}`)}</span>
